# ESP-IDF Partition Table — ESP32-S3, 16 MB flash
#
# Selected by the board, not by the project: esp32s3_n16r8's sdkconfig.defaults
# names this file in CONFIG_PARTITION_TABLE_CUSTOM_FILENAME. There is one board
# today and the file is still named after the flash size it assumes, because a
# table that fits 16 MB is the thing being selected — a second board with a
# different flash gets its own file rather than an edit to this one.
#
# No www partition: the built frontend is packed into one blob and linked into
# the app image (main/strux/WebAssets/), so a firmware image is the whole
# product and the UI needs no space of its own.
#
# ── Why the OTA slots are 3 MB and not 6 ──────────────────────
# Measured, not guessed. The app is 1,273,696 bytes (1.21 MiB) today — the whole
# framework plus the embedded web UI — so a 3 MiB slot is about 2.5x what is
# there, and leaves room for what is coming and is known to be big: an SVG
# renderer, a USB host stack, and the buffers that go with them. It is also the
# size at which growing the app 2.5x is still an ordinary OTA rather than a
# partition-table migration on every device in the field.
#
# Bigger would cost the data area twice over, because there are two slots, and
# 16 MB is not by itself a reason to spend it. If the app ever genuinely needs
# more, that is a decision with a measurement behind it, made then.
#
# ── storage: the data FAT ─────────────────────────────────────
# 9.875 MB, for label SVGs, media definitions and fonts. Declared FAT here so
# the space is named and the OTA slots cannot quietly grow into it, but NOTHING
# in this build mounts it, formats it or knows it exists: fatfs and
# wear_levelling are not in COMPONENT_REQUIRES and there is no mount call. It
# is reserved, not live.
#
# Name,     Type,   SubType,    Offset,     Size,       Flags
nvs,        data,   nvs,        0x9000,     0x6000,
otadata,    data,   ota,        0xf000,     0x2000,
phy_init,   data,   phy,        0x11000,    0x1000,
ota_0,      app,    ota_0,      0x20000,    0x300000,
ota_1,      app,    ota_1,      0x320000,   0x300000,
storage,    data,   fat,        0x620000,   0x9E0000,
