{
  "name": "lablr-render",
  "private": true,
  "type": "module",
  "description": "Headless renderer: runs the PWA's render code (lablr-ui) in Node via node-canvas, returning the DYMO raster job. One renderer, two hosts.",
  "scripts": {
    "build": "esbuild server.ts --bundle --platform=node --target=node20 --format=esm --packages=external --outfile=dist/server.mjs"
  },
  "dependencies": {
    "canvas": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
