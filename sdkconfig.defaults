# ── What is NOT here: flash size and the partition table ──────
# Both are board facts, not project facts, and asserting them here made the
# board overlay unable to disagree. The overlay wins on conflicts, so a 16 MB
# board setting CONFIG_ESPTOOLPY_FLASHSIZE_16MB left this file's 4 MB line not
# in effect — and the drift guard in CMakeLists.txt, which checks every line of
# every composed defaults file, correctly refused the build for it. The guard
# was right; the assertion was in the wrong file.
#
# So each board's sdkconfig.defaults now carries its own
#   CONFIG_ESPTOOLPY_FLASHSIZE_*MB
#   CONFIG_PARTITION_TABLE_CUSTOM_FILENAME
# and every board must set both — there is no default to inherit, which is
# deliberate: a board that forgets gets IDF's 2 MB default and a table that
# does not fit, rather than another board's layout by accident.
#
# The tables themselves stay at the project root (partitions-16mb.csv) rather
# than in the board folders: a table can be shared by several boards with the
# same flash, so what is board-specific is the CHOICE, and that is what the
# overlay holds.

# Custom table for every board; which file is the board's business (above).
CONFIG_PARTITION_TABLE_CUSTOM=y

# Flash speed, which no board disagrees with yet. A board that needs another
# one overrides it in its overlay and removes this line from here, the same
# way the size moved out.
CONFIG_ESPTOOLPY_FLASHFREQ_80M=y

# HTTP server WebSocket support
CONFIG_HTTPD_WS_SUPPORT=y

# Increase timer task stack (default 2048 too small for WiFi calls in timer callbacks)
CONFIG_FREERTOS_TIMER_TASK_STACK_DEPTH=4096

# ── FAT, for the label filesystem ─────────────────────────────
# The storage partition holds /labels, /fonts and /media. Long filenames are
# not optional here: a label is named by a human or by a model, and 8.3 would
# silently mangle every one of them.
CONFIG_FATFS_LFN_HEAP=y
CONFIG_FATFS_MAX_LFN=255

# ── ThorVG, the SVG renderer ──────────────────────────────────
# TTF is the whole point of the proof: an SVG <text> element resolves its
# font-family through ThorVG's font registry, so without the sfnt loader a
# label can draw shapes and no words. It is default-n upstream, hence this.
#
# Lottie is default-y upstream and dropped here - this device renders labels,
# not animations, and it is the largest loader of the set.
#
# Threads stay ON, and not by choice: ThorVG's own meson.build links pthread
# unconditionally on any non-Windows host, while the Espressif component only
# puts the pthread library on the linker path when THORVG_THREAD_ENABLED is y.
# Turning it off - which the Kconfig help invites - fails the build with
# "C++ shared or static library 'pthread' not found". So the pool is compiled
# in and asked for zero threads instead: tvg_engine_init(0) in RenderManager,
# which means rendering still happens on the task that ran the command.
#
# Log on for now. It is how a font that failed to load says so - ThorVG
# reports that through TVGLOG and returns a plain error code otherwise.
CONFIG_THORVG_SVG_LOADER_SUPPORT=y
CONFIG_THORVG_TTF_LOADER_SUPPORT=y
CONFIG_THORVG_LOG_ENABLED=y
# CONFIG_THORVG_LOTTIE_LOADER_SUPPORT is not set
CONFIG_THORVG_THREAD_ENABLED=y

# OTA rollback — pairs with esp_ota_mark_app_valid_cancel_rollback() in main.cpp
CONFIG_BOOTLOADER_APP_ROLLBACK_ENABLE=y

# Full speed, and no power management at all. This is a mains-powered label
# printer: it is plugged into a DYMO over USB, it has no battery, and the thing
# it does between long idle periods is rasterise SVGs — which is pure CPU, and
# which a person is waiting on. There is nothing here to trade latency for.
#
# 240 MHz rather than the S3's 160 MHz default, because the rasteriser is the
# device's slowest visible operation and it scales directly with clock.
CONFIG_ESP_DEFAULT_CPU_FREQ_MHZ_240=y

# Dynamic frequency scaling, deliberately OFF — this reverses an earlier decision
# (docs/reasoning/2026-09-02-15h52 and -15h53) and the reasoning that supported it
# still stands; what changed is the product, not the analysis. DFS dropped the CPU
# toward the XTAL whenever no task held a PM lock, which on a device waiting for a
# request was nearly all the time, and it cost a few microseconds of interrupt
# latency and coarser tick timekeeping to save milliamps nobody is counting.
#
# Worth keeping from that analysis, because it bounds what turning this back on
# could ever buy: WiFi holds an APB_FREQ_MAX lock that WIFI_PS_NONE (set
# deliberately, see WiFiInterface.cpp) never releases, so with the radio up the
# floor was 80 MHz rather than the XTAL's 40 — the saving was 160 -> 80, not
# 160 -> 40.
#
# Written in the "is not set" form rather than as =n on purpose: the drift guard
# in CMakeLists.txt checks CONFIG_X=... lines against the generated sdkconfig, and
# a disabled option is spelled as a comment there, so =n would be reported forever
# as an assertion that did not take.
# CONFIG_PM_ENABLE is not set
#
# FREERTOS_USE_TICKLESS_IDLE stays off too, and now trivially so: it only ever
# mattered as a prerequisite for light sleep, which is a battery-powered fork's
# decision and was never this one's.

# lwIP socket budget, and the reason it cannot stay at IDF's default of 10.
#
# esp_http_server reserves 3 sockets for itself on top of `max_open_sockets`, whose
# default is 7, and it refuses to start if LWIP_MAX_SOCKETS < max_open_sockets + 3.
# At the default of 10 that inequality is exactly tight: the web server is entitled
# to the WHOLE budget, and every other socket on the device — the relay's outbound
# WebSocket, mDNS, SNTP, the DHCP client — is competing for sockets httpd has already
# claimed. `accept()` then fails with errno 23 (ENFILE) and the server RESETS whichever
# connections lose the race.
#
# That stayed invisible while a page load asked for two files. It stopped being
# invisible when the UI briefly shipped as separate module bundles behind an import
# map: four concurrent requests plus the command WebSocket, and the browser silently
# lost the stylesheet, so the page came up with no CSS at all. Nothing in the UI could
# say why — the only evidence was `httpd_accept_conn: error in accept (23)` in the
# device log.
#
# The frontend is one bundle again, so a page load is back to three requests and this
# would no longer bite. The setting stays at 16 anyway — the option's maximum — because
# the inequality was always too tight: httpd's 7 + 3 against a budget of 10 leaves
# nothing for the relay's outbound socket, mDNS, SNTP or DHCP. A fork that adds a
# second listener should read this before trusting the default again.
CONFIG_LWIP_MAX_SOCKETS=16
